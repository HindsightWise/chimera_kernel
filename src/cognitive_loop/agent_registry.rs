use std :: collections :: { HashMap , HashSet } ; use tokio :: sync :: RwLock ; use tokio :: sync :: { mpsc , oneshot } ; use uuid :: Uuid ; use anyhow :: { Result , bail } ; use std :: sync :: Arc ; use crate :: cognitive_loop :: agent_trait :: { Agent , AgentCapability , Task , TaskResult } ; use crate :: cognitive_loop :: message_bus :: Message ; pub struct TaskEnvelope { pub task : Task , pub respond_to : oneshot :: Sender < Result < TaskResult > > , } pub struct MessageEnvelope { pub message : Message , pub respond_to : oneshot :: Sender < Result < () > > , } pub enum ActorCommand { ExecuteTask (TaskEnvelope) , HandleMessage (MessageEnvelope) , SubscribeToTopics (Arc < crate :: cognitive_loop :: message_bus :: MessageBus > , oneshot :: Sender < Result < () > >) , } pub struct AgentHandle { pub capabilities : HashSet < AgentCapability > , pub max_tasks : usize , pub command_tx : mpsc :: Sender < ActorCommand > , } pub struct AgentRegistry { agents : RwLock < HashMap < Uuid , AgentHandle > > , capability_index : RwLock < HashMap < AgentCapability , HashSet < Uuid > > > , } impl AgentRegistry { pub fn new () -> Self { Self { agents : RwLock :: new (HashMap :: new ()) , capability_index : RwLock :: new (HashMap :: new ()) , } } # [doc = " Register an agent by taking ownership of the Box<dyn Agent> and dropping it into an infinite tokio loop queue (Actor Model)"] pub async fn register (& self , mut agent : Box < dyn Agent >) -> Result < () > { let id = agent . id () ; let capabilities = agent . capabilities () . clone () ; let max_tasks = agent . max_concurrent_tasks () ; let mut agents_lock = self . agents . write () . await ; if agents_lock . contains_key (& id) { bail ! ("Agent with ID {} already registered" , id) ; } let (tx , mut rx) = mpsc :: channel :: < ActorCommand > (100) ; agents_lock . insert (id , AgentHandle { capabilities : capabilities . clone () , max_tasks , command_tx : tx , }) ; let agent_id = id ; tokio :: spawn (async move { crate :: log_ui ! ("Agent Actor {} Online" , agent_id) ; let mut bus_rx : Option < tokio :: sync :: broadcast :: Receiver < Message > > = None ; loop { if let Some (ref mut brx) = bus_rx { tokio :: select ! { cmd_opt = rx . recv () => { match cmd_opt { Some (cmd) => { match cmd { ActorCommand :: ExecuteTask (env) => { let res = agent . execute_task (env . task) . await ; let _ = env . respond_to . send (res) ; } ActorCommand :: HandleMessage (env) => { let res = agent . handle_message (env . message) . await ; let _ = env . respond_to . send (res) ; } ActorCommand :: SubscribeToTopics (bus , respond_to) => { match agent . subscribe_to_topics (bus) . await { Ok (new_rx) => { bus_rx = Some (new_rx) ; let _ = respond_to . send (Ok (())) ; } Err (e) => { let _ = respond_to . send (Err (e)) ; } } } } } None => break , } } msg_res = brx . recv () => { match msg_res { Ok (msg) => { let _ = agent . handle_message (msg) . await ; } Err (tokio :: sync :: broadcast :: error :: RecvError :: Closed) => { bus_rx = None ; } Err (tokio :: sync :: broadcast :: error :: RecvError :: Lagged (skipped)) => { crate :: log_ui_err ! ("Agent {} lagged and dropped {} messages!" , agent_id , skipped) ; } } } } } else { if let Some (cmd) = rx . recv () . await { match cmd { ActorCommand :: ExecuteTask (env) => { let res = agent . execute_task (env . task) . await ; let _ = env . respond_to . send (res) ; } ActorCommand :: HandleMessage (env) => { let res = agent . handle_message (env . message) . await ; let _ = env . respond_to . send (res) ; } ActorCommand :: SubscribeToTopics (bus , respond_to) => { match agent . subscribe_to_topics (bus) . await { Ok (new_rx) => { bus_rx = Some (new_rx) ; let _ = respond_to . send (Ok (())) ; } Err (e) => { let _ = respond_to . send (Err (e)) ; } } } } } else { break ; } } } }) ; let mut index = self . capability_index . write () . await ; for capability in capabilities { index . entry (capability) . or_insert_with (HashSet :: new) . insert (id) ; } Ok (()) } pub async fn unregister (& self , agent_id : Uuid) -> Result < () > { let capabilities = { let agents = self . agents . read () . await ; if let Some (handle) = agents . get (& agent_id) { handle . capabilities . clone () } else { return Ok (()) ; } } ; let mut agents = self . agents . write () . await ; agents . remove (& agent_id) ; let mut index = self . capability_index . write () . await ; for capability in capabilities { if let Some (agent_set) = index . get_mut (& capability) { agent_set . remove (& agent_id) ; if agent_set . is_empty () { index . remove (& capability) ; } } } Ok (()) } pub async fn find_capable_agents (& self , required_capabilities : & HashSet < AgentCapability >) -> Vec < Uuid > { let agents = self . agents . read () . await ; let index = self . capability_index . read () . await ; for capability in required_capabilities { if ! index . contains_key (capability) { return Vec :: new () ; } } let mut candidate_agents : Option < HashSet < Uuid > > = None ; for capability in required_capabilities { if let Some (agent_set) = index . get (capability) { candidate_agents = match candidate_agents . take () { Some (mut existing) => { existing . retain (| id | agent_set . contains (id)) ; Some (existing) } None => Some (agent_set . clone ()) , } ; if candidate_agents . as_ref () . map_or (false , | set | set . is_empty ()) { return Vec :: new () ; } } } let mut result = Vec :: new () ; if let Some (candidates) = candidate_agents { for agent_id in candidates { if agents . contains_key (& agent_id) { result . push (agent_id) ; } } } result } pub async fn get_agent (& self , _agent_id : Uuid) -> Option < Arc < RwLock < Box < dyn Agent > > > > { None } pub async fn get_agent_command_tx (& self , agent_id : Uuid) -> Option < mpsc :: Sender < ActorCommand > > { let agents = self . agents . read () . await ; agents . get (& agent_id) . map (| h | h . command_tx . clone ()) } pub async fn agent_count (& self) -> usize { let agents = self . agents . read () . await ; agents . len () } pub async fn all_agent_ids (& self) -> Vec < Uuid > { let agents = self . agents . read () . await ; agents . keys () . cloned () . collect () } pub async fn health_check_all (& self) -> HashMap < Uuid , bool > { let mut results = HashMap :: new () ; let agents = self . agents . read () . await ; for (id , _) in agents . iter () { results . insert (* id , true) ; } results } pub async fn dispatch_message (& self , agent_id : Uuid , message : Message) -> Result < () > { let option_tx = self . get_agent_command_tx (agent_id) . await ; if let Some (tx) = option_tx { let (resp_tx , resp_rx) = oneshot :: channel () ; let _ = tx . send (ActorCommand :: HandleMessage (MessageEnvelope { message , respond_to : resp_tx })) . await ; resp_rx . await ? } else { bail ! ("Agent {} not found" , agent_id) } } pub async fn initialize_agent_subscriptions (& self , message_bus : Arc < crate :: cognitive_loop :: message_bus :: MessageBus >) -> Result < () > { let ids = self . all_agent_ids () . await ; for id in ids { if let Some (tx) = self . get_agent_command_tx (id) . await { let (resp_tx , resp_rx) = oneshot :: channel () ; let _ = tx . send (ActorCommand :: SubscribeToTopics (message_bus . clone () , resp_tx)) . await ; let _ = resp_rx . await ? ; } } Ok (()) } pub async fn execute_task_on_agent (& self , agent_id : Uuid , task : Task) -> Result < TaskResult > { let option_tx = self . get_agent_command_tx (agent_id) . await ; if let Some (tx) = option_tx { let (resp_tx , resp_rx) = oneshot :: channel () ; tx . send (ActorCommand :: ExecuteTask (TaskEnvelope { task , respond_to : resp_tx })) . await . map_err (| _ | anyhow :: anyhow ! ("Agent {} Receiver dropped. Actor perished unexpectedly." , agent_id)) ? ; resp_rx . await . map_err (| _ | anyhow :: anyhow ! ("Oneshot channel dropped before Agent {} replied." , agent_id)) ? } else { bail ! ("Agent {} not found or unregistered" , agent_id) } } pub async fn get_agent_capabilities (& self , agent_id : Uuid) -> Option < HashSet < AgentCapability > > { let agents = self . agents . read () . await ; agents . get (& agent_id) . map (| h | h . capabilities . clone ()) } }