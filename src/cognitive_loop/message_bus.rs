use serde :: { Serialize , Deserialize } ; use uuid :: Uuid ; use tokio :: sync :: broadcast ; use chrono :: { DateTime , Utc } ; use anyhow :: Result ; # [derive (Debug , Clone , Serialize , Deserialize)] pub struct Message { pub id : Uuid , pub topic : String , pub payload : serde_json :: Value , pub sender : Uuid , pub timestamp : DateTime < Utc > , pub priority : u8 , pub ttl_secs : Option < u64 > , } # [derive (Debug , Clone , Serialize , Deserialize)] pub struct Subscription { pub subscriber_id : Uuid , pub topic_pattern : String , pub created_at : DateTime < Utc > , } pub struct MessageBus { pub tx : broadcast :: Sender < Message > , } impl MessageBus { pub fn new (_max_store_size : usize) -> Self { let (tx , _) = broadcast :: channel (1024) ; Self { tx , } } # [doc = " Subscribe to the broadcast channel"] pub fn subscribe (& self) -> broadcast :: Receiver < Message > { self . tx . subscribe () } # [doc = " Publish a message"] pub async fn publish (& self , message : Message) -> Result < () > { let _ = self . tx . send (message) ; Ok (()) } }