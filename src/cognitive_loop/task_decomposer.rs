use crate :: cognitive_loop :: agent_trait :: { AgentCapability , Task } ; use chrono :: Utc ; use colored :: Colorize ; use std :: collections :: { HashSet , VecDeque } ; use uuid :: Uuid ; pub struct DecompositionPattern { pub keyword : String , pub subtask_types : Vec < String > , } pub struct CapabilityGraph { pub capabilities : HashSet < AgentCapability > , } pub struct TaskDecomposer { pub historical_patterns : VecDeque < DecompositionPattern > , pub capability_graph : CapabilityGraph , } impl TaskDecomposer { pub fn new () -> Self { let mut tg = TaskDecomposer { historical_patterns : VecDeque :: new () , capability_graph : CapabilityGraph { capabilities : HashSet :: new () , } , } ; tg . historical_patterns . push_back (DecompositionPattern { keyword : "audit repo" . to_string () , subtask_types : vec ! ["stealth_scan" . to_string () , "gitnexus_blast_radius" . to_string () , "tavily_search" . to_string () ,] , }) ; tg . historical_patterns . push_back (DecompositionPattern { keyword : "market analysis" . to_string () , subtask_types : vec ! ["tavily_search" . to_string () , "axiom_clepsydra_extract" . to_string () ,] , }) ; tg } pub async fn decompose (& self , instruction : & str , parent_priority : u8) -> Vec < Task > { let mut subtasks = Vec :: new () ; let instruction_lower = instruction . to_lowercase () ; if instruction_lower . contains ("hello") || instruction_lower . contains ("echo") { let mut reqs = HashSet :: new () ; reqs . insert (AgentCapability :: Communication) ; let task_id = Uuid :: new_v4 () ; subtasks . push (Task { id : task_id , task_type : "basic_echo" . to_string () , payload : serde_json :: json ! ({ "instruction" : instruction }) , required_capabilities : reqs , priority : parent_priority , dependencies : vec ! [] , created_at : Utc :: now () , timeout_secs : Some (300) , geometric_node : [0.0 , 0.0 , 1.0] , topological_depth : 1 , execution_attempts : 0 , }) ; return subtasks ; } if let Ok (oracle) = crate :: core_identity :: duality :: Oracle :: new () . await { let prompt = format ! (r#"
    You are the Swarm Task Decomposer. Break down the following user intent into an array of concrete subtasks.
    Instruction: {}
    
    Return ONLY raw JSON conforming to this array format:
    [
      {{
        "task_type": "string (e.g. tavily_search, python_execution, code_review)",
        "capability": "ToolExecution|Reasoning|Security|MemoryManagement|Trading|Communication",
        "depends_on": [] // array of integers referencing the index of prerequisites. Leave empty if root node.
      }}
    ]
    "# , instruction) ; if let Ok (parsed) = oracle . synthesize_structured ("Decompose Subtasks into Semantic DAG" , & prompt) . await { if let Some (arr) = parsed . as_array () { let mut ref_map = std :: collections :: HashMap :: new () ; for (idx , _) in arr . iter () . enumerate () { ref_map . insert (idx , Uuid :: new_v4 ()) ; } for (idx , item) in arr . iter () . enumerate () { let t_type = item . get ("task_type") . and_then (| v | v . as_str ()) . unwrap_or ("research_basic") ; let cap_str = item . get ("capability") . and_then (| v | v . as_str ()) . unwrap_or ("Reasoning") ; let empty_vec = Vec :: new () ; let depends = item . get ("depends_on") . and_then (| v | v . as_array ()) . unwrap_or (& empty_vec) ; let mut reqs = HashSet :: new () ; match cap_str { "ToolExecution" => { reqs . insert (AgentCapability :: ToolExecution) ; } "Reasoning" => { reqs . insert (AgentCapability :: Reasoning) ; } "Security" => { reqs . insert (AgentCapability :: Security) ; } "MemoryManagement" => { reqs . insert (AgentCapability :: MemoryManagement) ; } "Trading" => { reqs . insert (AgentCapability :: Trading) ; } "Communication" => { reqs . insert (AgentCapability :: Communication) ; } _ => { reqs . insert (AgentCapability :: Reasoning) ; } } let mut dependencies = Vec :: new () ; for dep_val in depends { if let Some (dep_idx) = dep_val . as_u64 () { if let Some (dep_uuid) = ref_map . get (& (dep_idx as usize)) { dependencies . push (* dep_uuid) ; } } } let topological_depth = if dependencies . is_empty () { 1 } else { 2 } ; let radius = if topological_depth == 1 { 1.0 } else { 0.33 } ; let (mut x , mut y , mut z) = (0.0 , 0.0 , 0.0) ; if reqs . contains (& AgentCapability :: ToolExecution) { x = radius ; } else if reqs . contains (& AgentCapability :: Reasoning) { x = - radius ; } else if reqs . contains (& AgentCapability :: Security) { y = radius ; } else if reqs . contains (& AgentCapability :: Trading) { y = - radius ; } else { z = radius ; } let task_id = ref_map . get (& idx) . unwrap () ; subtasks . push (Task { id : * task_id , task_type : t_type . to_string () , payload : serde_json :: json ! ({ "instruction" : instruction }) , required_capabilities : reqs , priority : parent_priority , dependencies , created_at : Utc :: now () , timeout_secs : Some (300) , geometric_node : [x , y , z] , topological_depth , execution_attempts : 0 , }) ; } crate :: log_ui ! ("{}" , format ! ("[DECOMPOSER] Extracted {} tasks from Oracle generation." , subtasks . len ()) . magenta () . bold ()) ; return subtasks ; } else { crate :: log_ui_err ! ("{}" , "[DECOMPOSER] JSON parse structure failed: not an array or invalid format") ; } } } crate :: log_ui_err ! ("{}" , "[DECOMPOSER] Oracle unavailable or parsing failed. Falling back to simple default map.") ; let mut reqs = HashSet :: new () ; reqs . insert (AgentCapability :: Reasoning) ; subtasks . push (Task { id : Uuid :: new_v4 () , task_type : "research_basic" . to_string () , payload : serde_json :: json ! ({ "instruction" : instruction }) , required_capabilities : reqs , priority : parent_priority , dependencies : vec ! [] , created_at : Utc :: now () , timeout_secs : Some (300) , geometric_node : [- 1.0 , 0.0 , 0.0] , topological_depth : 1 , execution_attempts : 0 , }) ; subtasks } }