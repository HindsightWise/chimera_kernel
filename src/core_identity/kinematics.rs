use tokio :: fs ; use std :: path :: Path ; pub struct KinematicCortex ; impl KinematicCortex { pub async fn get_kinematics_for_tools (active_tools : & [async_openai :: types :: ChatCompletionTool]) -> String { let mut cortex_block = String :: new () ; for tool in active_tools { let tool_name = & tool . function . name ; let path_str = format ! ("KINEMATICS/kinematic_{}.md" , tool_name) ; let path = Path :: new (& path_str) ; if path . exists () { if let Ok (content) = fs :: read_to_string (path) . await { cortex_block . push_str (& format ! ("- **{}**:\n{}\n\n" , tool_name , content . trim ())) ; } } } cortex_block } pub async fn formulate_axiom (tool_name : & str , axiom : & str) -> anyhow :: Result < () > { let path_str = format ! ("KINEMATICS/kinematic_{}.md" , tool_name) ; let mut file = fs :: OpenOptions :: new () . create (true) . append (true) . open (& path_str) . await ? ; use tokio :: io :: AsyncWriteExt ; let formatted = format ! ("- {}\n" , axiom) ; file . write_all (formatted . as_bytes ()) . await ? ; Ok (()) } }